<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Beataroni.ViewModels;assembly=Beataroni"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Beataroni.Views.ModsView">
  <Design.DataContext>
    <vm:ModsViewModel/>
  </Design.DataContext>

  <!-- avalonia views can only contain 1 child, usually a Panel containing other controls -->
  <DockPanel>
    <TextBlock DockPanel.Dock="Top" Text="Mods" HorizontalAlignment="Center" />
    <!-- <TextBlock Text="Test Text: {Binding TestText}" HorizontalAlignment="Center" /> -->
    <!--
    <Button DockPanel.Dock="Bottom"
      Command="{Binding $parent[Window].DataContext.AddNewItem}">
      Add an item</Button>-->
    <ScrollViewer>
      <ItemsControl Items="{Binding Mods}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding name}"/>
  <!--
            <CheckBox Margin="4"
              IsChecked="{Binding IsDone}"
              Content="{Binding Description}"/>-->
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </DockPanel>
</UserControl>
